export const BRAND_NAME = "Liquidation"

const BRAND_LOGO_PATH = "/images/liquidation-logo.png"

/**
 * Bump when you replace `public/images/liquidation-logo.png` but keep the same filename.
 * Same URL otherwise keeps a cached image from `next/image` and the browser.
 */
export const BRAND_LOGO_REVISION = "2"

export const BRAND_LOGO_SRC = `${BRAND_LOGO_PATH}?v=${BRAND_LOGO_REVISION}`
export const BRAND_LOGO_WIDTH = 2000
export const BRAND_LOGO_HEIGHT = 500
